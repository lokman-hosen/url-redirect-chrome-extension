$(function () {



   /* chrome.tabs.getSelected(null,function(tab) {
        var tablink = tab.url;
        alert(tablink)
    });

    chrome.tabs.update({url: "https://facebook.com"});
    window.close();*/

});